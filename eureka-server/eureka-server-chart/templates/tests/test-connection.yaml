apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "eureka-server-chart.fullname" . }}-test-connection"
  labels:
    {{- include "eureka-server-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "eureka-server-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
